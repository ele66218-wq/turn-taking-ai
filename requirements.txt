# Pinned-by-floor requirements for a plain `pip install -r requirements.txt` setup.
# See pyproject.toml for optional extras (mic / embeddings / dev).
numpy>=1.26,<3
scipy>=1.11
scikit-learn>=1.4
joblib>=1.3
soundfile>=0.12
PyYAML>=6.0
sounddevice>=0.4   # microphone capture (recording / realtime only)
pytest>=8.0        # dev
